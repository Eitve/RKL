import React from 'react';
import { View } from 'react-native';

export default function StatisticsScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      {/* Intentionally blank view for Statistics */}
    </View>
  );
}
